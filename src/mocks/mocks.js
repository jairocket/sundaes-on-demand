// import { setupWorker, rest } from "msw";

// const worker = setupWorker(
//   rest.post("http://localhost:3030/order", (req, res, ctx) => {
//     const orderNumber = Math.floor(Math.random() * 1000000000);
//     return res(ctx.status(201).json({ orderNumber }));
//   })
// );

// worker.start();
